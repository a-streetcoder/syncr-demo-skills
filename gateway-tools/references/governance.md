# Why calls get refused
The gateway is fail-closed: a call runs only if the org approved it (or your admin granted you a
per-user allow). Unknown calls are recorded for review automatically — asking once through
request_tool_access is faster than asking a human to find the console.
