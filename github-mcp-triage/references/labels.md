# Label taxonomy
- `type:AFK` — an agent can complete it end-to-end without a human.
- `type:HITL` — needs accounts, credentials, or a judgment call.
- `phase:*` — exactly one; the roadmap phase that owns the work.
- `needs-triage` — entry state; remove once classified.
