# Severity rules
- **S1** — data loss, cross-tenant disclosure, or auth bypass: label `security-review`, ping the owner.
- **S2** — a user-visible feature broken with no workaround.
- **S3** — degraded but working; batch into the next slice.
